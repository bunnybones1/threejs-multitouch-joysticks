var three = require('three');
var TestPlane = require('./TestPlane');

function Pool(num, asset){ 
   var poolees = [];
      switch(asset) {
         case "testPlane":
            console.log('testPlane');
            poolees = createTestPlanes(num);
            break;

         default: 
            console.log('default');
            break;

   }
   
   function aquire()  {
      console.log('aquired');
   } 

   function release() {
     console.log("released"); 
  }
   
   return {
      aquire: aquire,
      release: release, 
      poolees
   }
}

function createTestPlanes(num) {
   var t = [];
   for (var i=0; i <= num; i++) {
      var testPlane = new TestPlane();
      t.push(testPlane);
   }
   return t;
}



module.exports = Pool;