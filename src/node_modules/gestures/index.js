var initd = false;

var touch = require('input/touch');

var draw = require('./draw');

function init() {
	if(initd) return;
	initd = true;

	touch.registerHandler(draw);
	module.exports.draw = draw;

	delete module.exports.init;
}

module.exports = {
	init: init
};