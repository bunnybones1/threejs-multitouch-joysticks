var initd = false;

var touch = require('input/touch');

var draw = require('./draw');
var shoot = require('./shoot');

function init() {
	if(initd) return;
	initd = true;

	touch.registerHandler(shoot);
	module.exports.shoot = shoot;

	delete module.exports.init;
}

module.exports = {
	init: init
};