var initd = false;

var touch = require('input/touch');

var draw = require('./draw');
var shoot = require('./shoot');
var walk = require('./walk');

function init() {
	if(initd) return;
	initd = true;

	touch.registerHandler(walk);
	module.exports.walk = walk;

	delete module.exports.init;
}

module.exports = {
	init: init
};