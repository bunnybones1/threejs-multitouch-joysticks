var initd = false;

var touch = require('./touch');
function init() {
	if(initd) return;
	initd = true;

	touch.init();
	module.exports.touch = touch;

	delete module.exports.init;
}

module.exports = {
	init: init
};