function RenderPass(scene, camera, clear) {
	this.scene = scene;
	this.camera = camera;
	this.clear = clear;
}

RenderPass.prototype.render = function(renderer) {
	renderer.render(this.scene, this.camera, undefined, this.clear);
};

module.exports = RenderPass;