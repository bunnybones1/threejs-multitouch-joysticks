// This is a base object with both visual and physics integrations
var defaults = require('lodash.defaults');

var defaultParams = {
	x: 0,
	y: 0,
	radius: 2,
	speed: 1
}

function Animal(params) { 
	params = params || {};
			speed
	defaults(params, defaultParams);

   var speed;
   
   function run() {}

   function death() {}

   function attack() {}

}

module.exports = Animal;