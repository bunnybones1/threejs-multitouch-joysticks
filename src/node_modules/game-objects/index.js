var initd = false;
var physics = require('physics');

var Hero = require('./Hero');

var world = require('world');
var view = require('view');

function init() {
	if(initd) return;
	initd = true;

	var scene = world.scene;

	var hero = new Hero();
	exports.hero = hero;
	physics.setAvatar(hero);

	delete exports.init;
}

var exports = {
	init: init
};

module.exports = exports;